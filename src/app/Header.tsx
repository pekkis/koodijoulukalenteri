import { FC } from "react";
import * as styles from "./Header.css";

const Header: FC = () => {
  return (
    <header className={styles.header}>Peksun koodi&shy;joulukalenteri</header>
  );
};

export default Header;
