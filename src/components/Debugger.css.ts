import { style } from "@vanilla-extract/css";

export const flexClass = style({
  display: "flex"
});

export const contentClass = style({
  flex: "10"
});

export const debuggerClass = style({
  flex: "2"
});
